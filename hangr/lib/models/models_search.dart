class PhotoSearch {
  final String imagePath;
  final String name;
  final String category; // For shirt/outer/accessory/pants
  final String season; // To decide the degree 
  PhotoSearch(this.imagePath, this.name, this.category, this.season);
}